spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/postgres
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: create



  mvc:
    hiddenmethod:
      filter:
        enabled: true